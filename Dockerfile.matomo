FROM matomo:latest

# Update packages and install nano
RUN apt-get update && apt-get install nano -y

# Modify the config.ini.php file
RUN echo "trusted_hosts[] = \"localhost:8081\"" >> /var/www/html/config/config.ini.php
