version: '3.9'

services:
  matomo:
    image: matomo
    ports:
      - '8081:80'
    depends_on:
      - mysql
    env_file:
      - .env
    volumes:
      - matomo_data:/var/www/html

  mysql:
    image: mysql:latest
    ports:
      - '5550:3306'
    env_file:
      - .env
    volumes:
      - mysql_data:/var/lib/mysql

  next:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/usr/src/app
    ports:
      - 3030:3030
    depends_on:
      - mysql
      - matomo

volumes:
  matomo_data:
  mysql_data:
